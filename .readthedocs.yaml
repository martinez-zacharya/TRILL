version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.8"

# conda:
#   environment: .environment.yml 

sphinx:
  configuration: docs/conf.py
  
 # python:
  # install:
    # - requirements: docs/requirements.txt
